lembrar de iniciar o env do python
`.\env\Scripts\Activate.ps1` pela raiz do projeto

lembrar de criar o database no mysql
`create schema fastapidatabase`

precisa instalar o bcrypt no pc de trampo ainda
`pip install bcrypt`

